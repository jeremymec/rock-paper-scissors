type signAmounts = [rocks: number, papers: number, scissors: number]

const STARTING_SIGNS: signAmounts = [10, 3, 3];

const CANVAS_WIDTH = 800;
const CANVAS_HEIGHT = 800;

const SIGN_WIDTH = 40;
const SIGN_HEIGHT = 40;

const STARTING_VELOCITY = 2;

export { STARTING_SIGNS, CANVAS_WIDTH, CANVAS_HEIGHT, SIGN_WIDTH, SIGN_HEIGHT, STARTING_VELOCITY }
